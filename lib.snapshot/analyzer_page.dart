import 'package:flutter/material.dart';

class AnalyzerPage extends StatelessWidget {
  const AnalyzerPage({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Analyzer home')));
  }
}
